# 🪪 那覇マイページ 引き継ぎ書（2026-07-08）

札幌マイページ（本番稼働中）を那覇へ横展開する作業の引き継ぎ。次セッション/CLIはこれを起点に続行する。
正本の詳細仕様は `~/spk-task/CLAUDE.md` の「🪪 統合マイページ」「🚀 マイページ 札幌 本番リリース」各節を参照。

## 🎨 表示を札幌に完全統一（2026-07-08 追加対応・本番反映済）
オーナー指摘「表示仕様が統一されてない／全て札幌に合わせて／KEYDROPも同一」→ **my-nha.html を札幌 my.html の丸写し＋最小差分に作り替え**、EF応答も札幌形に統一。
- **my-nha.html = `~/spk-task/my.html` の丸写し**。差分は4点のみ：①FN→`handyman-mypage-nha` ②`const RO=true`固定(内部閲覧専用) ③IMG_BASE→`https://nosh2318.github.io/spk-task/images/`(クラス画像`class_*_nha.png`が那覇でも出る) ④お届け・回収カードにバス分岐(`PUB/BDB`→bus.html時刻表 ／`来店/返却`→時間のみ ／`DEL/COL`→場所+時間)。他は全て札幌と同一レンダラ＝ヘッダー/言語切替/ヒーロー+クラス画像/ご予約内容/📍お届け回収/🩹傷/追跡/🕘履歴/📣お知らせ/フッターが完全一致。
- **EF `handyman-mypage-nha` lookup を札幌と同じ flat 応答形に統一**（`reservation.vehicle/del_place/col_place/lend_time/...` ＋ `visit_type/return_type`＋`damage/tracking/history/pendingCancel/readyPending`）。→ 同一レンダラで描けるようになった。
- **KEYDROP等 全OTA自動対応**：EFは ota 不問で `nha_reservations` を token で引く＝KEYDROP(KDN-)含め全予約が同じカルテに乗る。
- 検証：EF新応答＝vehicle=B/del_place=赤嶺駅南口/16:00/visit=DEL・return=BDB・history2件。本番 my-nha VER=`v6.4-nha`・RO固定・spk-task画像・バス時刻表 反映確認。⚠️ブラウザ拡張未接続でスクショ検証は未（オーナー実機で確認）。

## ✅✅ 一旦のゴール達成（2026-07-08 本番反映済・オーナー手作業なし）
内部カルテ閲覧まで **全て本番稼働**。顧客開示は範囲外（未実施）。
- **EF `handyman-mypage-nha`（lookum+ping・閲覧専用）デプロイ済**（ckrxttbnawkclshczsia）。正本 `~/spk-task/line_auto/handyman-mypage-nha/index.ts`。実データ検証OK（DEL=赤嶺駅南口16:00／PUB→BDBバス分岐＝OP一致）。
- **`my-nha.html` 本番200**：`https://nosh2318.github.io/naha-project/my-nha.html?t=<token>&ro=1`（standalone・閲覧専用・PUB/BDB→バス時刻表／DEL/COL→場所+時間／来店/返却→時間のみ）。VER=v1.0。
- **NHA本体 v3.5.264-NHA push済**：`window._mypageTokenMap`(nha_reservations.mypage_token) 生成 useEffect ＋ タスクサマリ/スケジュール行の免許証🪪の隣に **🪪カルテ(ro=1)** 導線（2箇所）。
- 検証token：`57c5abbe-…`(DEL赤嶺駅南口16:00) / `1ee801bc-…`(PUB=バス) / `5d3db425-…`(DEL牧志・返却=時間のみ)。
- 📌学び：**NHAアプリの現行anonキーは iat 1771878550 の版**。my系にキー埋込時は必ずapp本体から現行版を取る(旧版はRLSで0件)。NHA分岐実値=visit_type{PUB/DEL/来店/PU}・return_type{BDB/COL/返却/BD}、バス判定=PUB/BDB。

## ✅ 完了（Phase1・オーナー手作業なし）
- **DB土台**：`nha_reservations` に `mypage_token`(unique/default gen_random_uuid)・`mypage_locked`(jsonb)・`del_lat/del_lng/col_lat/col_lng` を追加済。
- **トークン発行**：那覇 全2,514予約にURL発行済。
- 共有ログ系（audit_log/mypage_changes 等）は既存 main DB(ckrxttbnawkclshczsia) に相乗り（札幌と同一テーブル、store列で分離）。

## 🔒 確定した表示ルール（オーナー確定・ロック）
受け渡し区分（visit_type/return_type）に応じて那覇のお客様ページの時間表示を切替：
- **PUB / BDB（空港バス）→ バス時刻表を表示**（那覇 `bus.html` を流用）
- **PU / BD（個別送迎）→ 個別の受け渡し時間を表示**
- **DEL（お届け）/ COL（回収）→ 場所＋個別の時間を表示**（変更もマイページ可・札幌と同じ）
- **来店 → 個別の時間のみ表示**

## 🎯 一旦のゴール（オーナー確定 2026-07-08）＝内部カルテ閲覧まで（顧客へは開示しない）
札幌と同様に、**スタッフがOPシート・タスクサマリから、お客様のマイページ＝カルテを閲覧専用(ro=1)で開ける**ところまで。顧客への通知/開示は今回は範囲外。核心は**情報連動**（カルテの場所/時間が OPシートと一致）。那覇は分岐が多いが情報は全てOPシート(nha_tasks)にある。

### 札幌の型（そのまま那覇へ写す）
- `window._mypageTokenMap`：`reservations.id + mypage_token` をページネーション取得（SPK index.src.html L20301）。
- OP表(L15956)＝🪪マイページリンク、タスクサマリDEL/COL行(L21601)＝`my.html?t=<token>&ro=1`（閲覧専用）。
- 閲覧専用は my.html が `ro` 検出で editable=false＋書込ブロック（既存実装）。

### 那覇の受け渡し分岐の表示（reservation.visit_type / return_type 起点）
- お届け側：`PUB`→バス時刻表 ／ `DEL`（＝個別送迎）→場所＋個別時間 ／ `来店`→時間のみ
- 回収側：`BDB`→バス時刻表 ／ `COL`→場所＋個別時間 ／ `返却`→時間のみ
- ※アプリ上のPU/BDラベルは内部値 `来店`/`返却`。バス判定は NHA本体の定数（index.html.bak L3843〜 マイクロバス運行/`NHA_PLACE_OPTIONS`）と `bus.html` を流用。

## 🧱 那覇の箱（実装に必要な事実）
- `nha_reservations`（_fromDbRes 準拠）：`start_date/start_time/end_date/end_time・vehicle_class・vehicle_name・insurance・del_place/del_time・col_place/col_time・visit_type・return_type・del_flight・opt_b/c/j/usb・status・mypage_token`。
- `nha_tasks`（日本語列・_fromDbTask 準拠）：`内容`(PUB/DEL/来店/洗車)・`時間`・`予約者`・`担当`・`予約番号`・`送迎場所`(place)・`集客`(colPlace)・`返却`(returnTime)・`送迎`(returnType)・`クラス`・`確定`(insurance)・`便名`・`changed_json`(内に `_ssPlace/_ssTime/_placeSource/_timeChange/_manual/_taskDate`)。PK `_id` は d-/c-/w- 接頭辞（2026-07-05〜）。
- 場所/時間の解決式（札幌と同思想）：`_placeSource==="manual"?place:(_ssPlace||place)` ／ `_timeChange||_ssTime||時間`。d-=お届け, c-=回収。
- デプロイ：naha-project は build.js（index.html.bak→app.js）＋ index.html の `BASE_V`（現3765）。my-nha.html は standalone（bus.html同様 push即反映）。

## 🚧 残タスク（重い開発・作る→デプロイ→実機テストの反復）
1. **Edge Function 那覇移植**（決済/変更/キャンセル/lookup）
   - 札幌版 `handyman-mypage` は全部SPK固定（英語列・SPKタスク `tasks` d-/c-/w-）。
   - 那覇は **日本語列・区分 PUB/PU/BD/BDB/来店/DEL/COL・`nha_tasks`** へ作り替える。
   - 正本 `~/spk-task/line_auto/handyman-mypage/index.ts` を複製元に那覇用を新設 → deploy実体 `~/hdm-car-delivery/supabase/functions/` にコピーして `--no-verify-jwt`。
   - lookup の場所/時間解決は「reservations と nha_tasks の実値ある方」を採る（札幌の `resolveTaskPlace/Time` 同思想）。
2. **お客様ページ my-nha.html**（那覇仕様）
   - 上記の表示ルール（バス時刻表 / 個別時間の切替）を組み込む。
   - クラス画像は那覇同梱（`images/class_*_nha` 等）。バス時刻表は `bus.html` 流用。
3. **管理コンソール my-admin（那覇対応）**：カテゴリ/承認フロー/整合パトロールを nha_reservations/nha_tasks に対応。
4. **LINE自動送信の那覇配線**：`nha_line_*`（別LINEアカウント @466dbckq・test_user_id=U2a777a5fc6310a32e4f93582515a490d）・`mypage-notify` 那覇cron。

## ⚠️ 注意
- 那覇の予約は2000超 → PostgREST 1000行上限。全件処理はページネーション必須。
- テスト送信は必ずオーナーの那覇チャネルuserID宛のみ。実顧客へ勝手に送らない。
- 那覇本体は build.js（index.html.bak → app.js）＋ BASE_V(index.html) 更新。sw.jsは0バイト運用。
- 推奨：Slack1往復より **CLI（`cd ~/spk-task && claude --agent omni`）で一気に回す方が速く安い**。
