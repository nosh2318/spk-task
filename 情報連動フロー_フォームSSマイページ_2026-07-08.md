# 情報整理の流れ ─ フォーム(SS)・OPシート・マイページ(カルテ) の連動とルール（2026-07-08）

## 1. 情報の置き場所（どこに何があるか）
| 箱 | 中身 | 位置づけ |
|---|---|---|
| ① フォーム(SS)／`{store}_line_links` | お客様がエルメ/LINE受付フォームで回答した 場所・時間（予約番号ごと） | 顧客回答の受け皿 |
| ② 予約DB `nha_reservations`/`reservations` | 予約の素データ（del_place/col_place・時間・区分 visit_type/return_type・OTA・クラス等） | 予約の土台 |
| ③ OPシート `nha_tasks`/`tasks`（changed_json） | 現場が見る確定値。`_ssPlace/_ssTime`(フォーム由来)・`_placeSource`(誰が決めたか)・`_timeChange`(変更) | **現場表示の正本** |
| ④ マイページ変更ログ `mypage_changes` | 顧客/スタッフがマイページで変更した記録（applied/requested） | **変更の追記正本** |
| ⑤ 台帳 `audit_log` | 全テーブルの全書込（改ざん不能） | 監査の正本 |

## 2. 連動の流れ（予約→表示）
1. **予約が入る** → 自動で `mypage_token` 生成（既定 gen_random_uuid＝全予約100%・KEYDROP含む）。この時点でカルテURLが1本できる。
2. **お客様がフォームで回答**（場所・時間）→ SS／`{store}_line_links` に入る（**予約番号 完全一致**で紐づく）。
3. **SSパトロール／取込** → OPタスク `③` の `_ssPlace/_ssTime` に反映（現場が見えるようになる）。
4. **マイページ(カルテ)表示** → EF `handyman-mypage-nha` の `lookup` が、②予約DB と ③OPタスク の**「実値のある方」**を解決して表示。
   - 場所 ＝ `_placeSource==="manual" ? place : (_ssPlace || place)`
   - 時間 ＝ `_timeChange || _ssTime || time`
5. **（将来・顧客開示ON時）お客様がマイページで変更** → `④mypage_changes`(applied) に記録＋③tasks に `_placeSource="customer"` / `_timeChange` を立てる → **以後マイページ優先**（フォーム再取込で戻らない）。

→ フォーム・OPシート・マイページの3画面が**同じ元データ（②＋③）から解決**するので常に一致する。

## 3. ルール（重要・違反禁止）
1. **予約番号「完全一致」以外は場所/時間を書かない＝空白**。O↔0 等の曖昧一致で別予約の値を当てにいかない（テキトーな値より空白が安全）。
2. **一度マイページで編集したらマイページ優先**。フォームの再取込・SSパトロールで戻さない（担保＝`_placeSource=customer`／`_timeChange`）。
3. **人間の手動編集（`_placeSource=manual`）は機械/CSV/再取込で上書きしない**。競合したら人間が勝つ。
4. **単一項目だけ書く。既存行を丸ごと再保存しない**（唯一ルール）。削除は墓標(deleted=true)。
5. **顧客は許可項目のみ**（場所/時間）。センシティブ（キャンセル/補償/オプション）は**承認制**（将来）。
6. **全書込は audit_log に記録**（誰が・いつ・何を）。

## 4. 那覇の受け渡し区分による表示分岐
フォームで「場所」の回答が要るのは **DEL/COL のみ**。空港送迎・来店は場所不要。
| 区分 | 意味 | カルテ表示 |
|---|---|---|
| DEL / COL | お届け / 回収（指定場所） | **お届け先/回収先＝場所＋時間**（フォーム回答が必要） |
| PU / PUB | 空港お迎え（PU=個別／PUB=送迎バス） | **空港お迎え**（PUB=🚌バス時刻表、時間のみ・場所不要） |
| BD / BDB | 空港お見送り（BD=個別／BDB=送迎バス） | **空港お見送り**（BDB=🚌バス時刻表、時間のみ・場所不要） |
| 来店 / 返却 | 店頭受け取り / 店頭返却 | **店頭でお受け取り/ご返却**（時間のみ） |

## 5. 現状（2026-07-08）
- スタッフがOPシート／タスクサマリの各予約行の **🪪カルテ** から閲覧専用(ro=1)で開ける（顧客開示・変更はまだ＝範囲外）。
- カルテの表示は札幌 my.html と統一（同一レンダラ・EF応答も同形）。
- 次段：LINE連携済みのお客様への開示（有効化するとお客様が自分で時間・場所変更／依頼が可能に）を**いつやるか**が論点。
